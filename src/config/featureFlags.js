/**
 * Feature Flags — controle de funcionalidades provisórias.
 *
 * Para reativar o motor de reservas (Omnibees), basta alterar
 * BOOKING_ENGINE_ENABLED para true.
 */

export const FEATURE_FLAGS = {
    /** Motor de reservas Omnibees (barra sticky inferior) */
    BOOKING_ENGINE_ENABLED: false,
};
